---
import { contact } from '../data/link'
---

<footer class="mt-56 wrapper flex-col" id="footer">
  <span class="border-b-[0.00625rem]"></span>
  <p class="mt-6 text-lg md:text-2xl">
    Feel free to reach out, I'm always ready to meet new people.
  </p>
  <span class="mt-24 border-b-[0.00625rem]"></span>
  <div class="flex flex-col md:flex-row mt-6 gap-10 md:gap-40">
    <div class="flex flex-col gap-1 text-sm md:text-xl">
      <p class="text-zinc-400">Email</p>
      <a
        href={`mailto:${contact.email}`}
        class="hover:text-white duration-200 transition-all font-medium"
        aria-label="Email us">{contact.email}</a
      >
    </div>
  </div>
  <span class="mt-24 border-b-[0.00625rem]"></span>
  <div class="mt-6 flex">
    {
      Object.entries(contact.social).map(([key, value]) => (
        <a
          href={value}
          target="_blank"
          class="text-sm md:text-base font-medium text-zinc-300 hover:text-white transition-colors duration-300 mr-6"
          aria-label={`Follow me on ${key}`}>
          {key.charAt(0).toUpperCase() + key.slice(1)}
        </a>
      ))
    }
  </div>
  <div class="mt-64 mb-10 text-xs md:text-sm font-light">
    &copy;{new Date().getFullYear()} AYUSHKUMAR
  </div>
</footer>
